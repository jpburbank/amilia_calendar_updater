# Real deploy config, built from env.yaml.example. Not secret today, but it is
# the file that grows secrets first.
env.yaml

__pycache__/
*.py[cod]
.pytest_cache/
.venv/
venv/
.DS_Store

# Generated by scripts/build.sh before each deploy — src/shared/ is the
# single source of truth, these are just deploy-time copies.
/src/webhook/shared/
/src/reconciler/shared/

# Per-run records written by scripts/bulk_delete.py — see scripts/BULK_DELETE.md.
/audit_logs/
