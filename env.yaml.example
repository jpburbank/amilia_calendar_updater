GOOGLE_CALENDAR_ID: "your-calendar-id@group.calendar.google.com"

# AMILIA_WEBHOOK_TOKEN is NOT set here — it's a secret, mounted at deploy
# time via --set-secrets from Secret Manager instead. See main.py's deploy
# docstring.
